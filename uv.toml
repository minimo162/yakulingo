native-tls = true
python-preference = "only-managed"
cache-dir = "./.uv-cache"

# Include pythonnet for native window mode on Windows
# Note: clr-loader may cause SSL certificate issues in corporate environments
[[dependency-metadata]]
name = "pywebview"
version = "6.1"
requires-dist = [
    "bottle",
    "proxy-tools",
    "typing-extensions",
    "pyobjc-core; sys_platform == 'darwin'",
    "pyobjc-framework-cocoa; sys_platform == 'darwin'",
    "pyobjc-framework-quartz; sys_platform == 'darwin'",
    "pyobjc-framework-security; sys_platform == 'darwin'",
    "pyobjc-framework-uniformtypeidentifiers; sys_platform == 'darwin'",
    "pyobjc-framework-webkit; sys_platform == 'darwin'",
    "qtpy; sys_platform == 'openbsd6'",
    "pythonnet; sys_platform == 'win32'",
]
