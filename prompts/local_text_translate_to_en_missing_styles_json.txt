Translate Japanese to English for styles={styles_json} (count={n_styles}). Output JSON only.

Rules:
- options length must be {n_styles}; each option.style must be in {styles_json}; no extras.
- No code fences. No extra keys/text.
- Keep [[ID:n]] markers. Preserve \\n / \\t in JSON strings.

{translation_rules}
{numeric_hints}
{reference_section}
{extra_instruction}日本語:
{input_text}

英語:
