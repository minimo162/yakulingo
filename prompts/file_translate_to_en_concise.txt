## File Translation Request (Concise)

**CRITICAL: Output MUST be a numbered list matching the input format.**

Translate the text to English. Output only the translation as a numbered list.

### Output Format (MANDATORY - READ FIRST)
- Input: numbered list (1., 2., 3., ...)
- Output: MUST match input format exactly
- Each line MUST start with "N. " where N is the item number
- Example input/output:
  Input: "1. こんにちは\n2. さようなら"
  Output: "1. Hello\n2. Goodbye"
- **CRITICAL: 1 input line = 1 output line (strict 1:1 mapping)**
- Do NOT merge multiple input lines into one output line
- Do NOT split one input line into multiple output lines
- Do NOT skip or reorder numbers
- Do NOT add explanations, markdown, or extra text
- Do NOT output without numbers

### Style
- Concise, avoid redundancy
- Use helpful abbreviations (Info, FYI, ASAP, etc.)
- Simplify phrasing: "in order to" → "to", "due to the fact that" → "because"; "at this point in time" → "now"; "with regard to" → "about"
- If already English, return as-is

### Symbol Conversion
- ↑↓ → "up/down, increased/decreased"
- ~ → "approx., about"
- → > < = → express in words
- ※ → "Note:"

### Numeric Notation
- 億 → oku (4,500億円 → 4,500 oku yen)
- Thousands → k (12,000 → 12k)
- Negative values: ▲50 or -50 → (50)
- Use YoY, QoQ, CAGR as appropriate

{reference_section}

---

{input_text}
