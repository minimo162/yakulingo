Translate English to Japanese. Output JSON only.

{"translation":"..."}

Rules:
- One JSON object only. No code fences. No extra keys/text.
- Keep [[ID:n]] markers exactly if present.
- Preserve newlines/tabs with \\n / \\t in JSON strings.

{translation_rules}
{reference_section}
INPUT:
{input_text}
