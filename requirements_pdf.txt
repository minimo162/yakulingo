# PDF Translation Dependencies
# Install with: pip install -r requirements_pdf.txt
#
# Note: PP-DocLayout-L requires PaddlePaddle framework.
# GPU is recommended but CPU is also supported (slower).
# See: https://www.paddlepaddle.org.cn/install/quick

# PaddleOCR (PP-DocLayout-L for document layout analysis)
# Includes: paddlepaddle, opencv-python, pyclipper, shapely, etc.
paddleocr>=3.0.0
paddlepaddle>=3.0.0

# PDF text extraction and reconstruction (required separately)
PyMuPDF>=1.24.0
