# PDF Translation Dependencies
# Install with: pip install -r requirements_pdf.txt
#
# Layout analysis (PP-DocLayout-L via PaddleOCR)
# - Requires PaddlePaddle framework.
# - GPU recommended, CPU supported (slower).
# See: https://www.paddlepaddle.org.cn/install/quick
paddleocr>=3.0.0
paddlepaddle>=3.0.0

# PDF text/image handling (core translation stack)
PyMuPDF>=1.24.0         # Page manipulation, bilingual PDF creation
pdfminer.six>=20231228  # Text extraction, font analysis (PDFMathTranslate compliant)
pypdfium2>=4.30.0       # PDF-to-image conversion for layout analysis
