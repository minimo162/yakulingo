## File Translation Request

**CRITICAL: Output MUST be a numbered list matching the input format.**

Translate the following text to Japanese. Output only the translation as a numbered list.

### Output Format (MANDATORY - READ FIRST)
- Input: numbered list (1., 2., 3., ...)
- Output: MUST match input format exactly
- Each line MUST start with "N. " where N is the item number
- Example input/output:
  Input: "1. Hello\n2. Goodbye"
  Output: "1. こんにちは\n2. さようなら"
- Do NOT skip, merge, or reorder numbers
- Do NOT add explanations, markdown, or extra text
- Do NOT output without numbers

### Guidelines
- Style: concise Japanese, avoid verbose expressions
- If already Japanese, output as-is

### Numeric Notation
- oku → 億 (4,500 oku → 4,500億)
- k → 千/000 (12k → 12,000)
- () → ▲ ((50) → ▲50)

{reference_section}

---

{input_text}
