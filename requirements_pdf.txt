# PDF Translation Dependencies
# Install with: pip install -r requirements_pdf.txt
#
# Note: yomitoku requires Python 3.10-3.12 and PyTorch 2.5+
# A GPU with 8GB+ VRAM is recommended for optimal performance.
# See: https://kotaro-kinoshita.github.io/yomitoku/en/installation/

# yomitoku (Japanese document AI - Layout analysis & OCR)
# Automatically installs: torch, torchvision, opencv-python, pypdfium2,
# pydantic, omegaconf, shapely, huggingface-hub, timm, onnx, onnxruntime, etc.
yomitoku>=0.10.0

# PDF text extraction and reconstruction (required separately)
PyMuPDF>=1.24.0
