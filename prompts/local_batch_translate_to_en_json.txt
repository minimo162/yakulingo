Translate each item.text from Japanese to English.
Style: {style}
Output JSON only: an object with key "items" (array of {n_items}).

Rules:
- No code fences. No extra text outside the JSON object.
- items length must be {n_items}; ids must be 1..{n_items}; each item must include "id" and "translation".
- Keep [[ID:n]] markers. Preserve \\n / \\t in JSON strings.

{translation_rules}
{numeric_hints}
{reference_section}
<source>
{items_json}
</source>
