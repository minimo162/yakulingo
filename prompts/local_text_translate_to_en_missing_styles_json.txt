Translate the following text from Japanese to English for styles={styles_json} (count={n_styles}).
Output JSON only: an object with key "options" (array of {n_styles}).

Rules:
- No code fences. No extra text outside the JSON object.
- options length must be {n_styles}; each item must include "style" (in {styles_json}) and "translation".
- Keep [[ID:n]] markers. Preserve \\n / \\t in JSON strings.

{translation_rules}
{numeric_hints}
{reference_section}
{extra_instruction}<source>
{input_text}
</source>
